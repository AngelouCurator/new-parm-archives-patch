# Never commit game data. This repo ships only the xdelta3 patch — no disc images,
# no extracted game text, no emulator savestates.
*.bin
*.cue
*.iso
*.chd
*.img
*.sscd
*.SSCD
*.savestate
*.zip

# Locally built helper used to validate SSP archives; not a release artifact.
/tools/saturn-patcher-egui-macos/

# OS / editor cruft
.DS_Store
*.swp
